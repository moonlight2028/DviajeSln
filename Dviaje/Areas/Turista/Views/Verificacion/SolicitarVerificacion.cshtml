@* @model Dviaje.Models.VM.VerificacionVM

<div class="container mt-5">
    <h2 class="text-center mb-4">Solicitar Verificación de Aliado</h2>

    <form asp-action="SolicitarVerificacion" method="post" enctype="multipart/form-data">
        

        <div class="form-group">
            <label for="Motivo">Motivo para ser aliado</label>
            <textarea asp-for="Motivo" class="form-control" rows="3"></textarea>
            <span asp-validation-for="Motivo" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label for="Direccion">Dirección del lugar</label>
            <input asp-for="Direccion" class="form-control" />
            <span asp-validation-for="Direccion" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label for="DocumentosAdjuntos">Documentos Adjuntos (Pruebas de propiedad)</label>
            <input asp-for="DocumentosAdjuntos" class="form-control" type="file" multiple />
            <span asp-validation-for="DocumentosAdjuntos" class="text-danger"></span>
        </div>

        <div class="form-group">
            <label for="DetallesPropiedad">Detalles adicionales sobre la propiedad</label>
            <textarea asp-for="DetallesPropiedad" class="form-control" rows="3"></textarea>
            <span asp-validation-for="DetallesPropiedad" class="text-danger"></span>
        </div>

        <button type="submit" class="btn btn-primary">Enviar Solicitud</button>
    </form>
</div>
 *@
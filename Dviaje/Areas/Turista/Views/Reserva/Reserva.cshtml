@* @model Dviaje.Models.Reserva

@{
    ViewData["Title"] = "Reservar";
}

<div class="container mt-5">
    <h2 class="text-center mb-4">Reserva tu Aventura</h2>
    <div class="row justify-content-center">
        <div class="col-md-8">
            <form asp-action="Reserva" method="post">
                <div class="card shadow-sm">
                    <div class="card-body">
                        <div class="form-group">
                            <label asp-for="FechaInicial" class="control-label"></label>
                            <input asp-for="FechaInicial" class="form-control" type="datetime-local" />
                            <span asp-validation-for="FechaInicial" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="FechaFinal" class="control-label"></label>
                            <input asp-for="FechaFinal" class="form-control" type="datetime-local" />
                            <span asp-validation-for="FechaFinal" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="NumeroPersonas" class="control-label"></label>
                            <input asp-for="NumeroPersonas" class="form-control" type="number" min="1" max="999" />
                            <span asp-validation-for="NumeroPersonas" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="IdUsuario" class="control-label"></label>
                            <input asp-for="IdUsuario" class="form-control" type="hidden" value="@User.Identity.Name" />
                            <span asp-validation-for="IdUsuario" class="text-danger"></span>
                        </div>
                        <div class="form-group">
                            <label asp-for="IdPublicacion" class="control-label"></label>
                            <input asp-for="IdPublicacion" class="form-control" type="hidden" />
                            <span asp-validation-for="IdPublicacion" class="text-danger"></span>
                        </div>
                        <button type="submit" class="btn btn-primary mt-3 w-100">Confirmar Reserva</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
 *@